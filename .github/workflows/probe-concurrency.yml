name: Probe concurrency
on: pull_request
concurrency:
  group: probe-${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
